

// tests/order_tests.cpp
#include <gtest/gtest.h>
#include "order_engine/core/order.hpp"

TEST(OrderTest, ConstructsWithCorrectFields) {
    // just assert true for now to prove the build works
    ASSERT_TRUE(true);
}